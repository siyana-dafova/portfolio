import React from "react";
import Section from "../../ui/Section";
import HeroContent from "./HeroContent";

const Hero = () => {
  return (
    <Section id="about">
      <HeroContent />
    </Section>
  );
};

export default Hero;
